name: CI

on:
  pull_request:
  push:
    branches:
      - main
    paths-ignore:
      - ".memd/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v7

      - name: Validate shell scripts
        run: find scripts skills -type f -name '*.sh' -print0 | xargs -0 -r bash -n

      - name: Validate skills
        run: python3 scripts/validate-skills.py

      - name: Check catalog is up to date with sources
        run: |
          python3 scripts/skill_index.py build --repo . --out catalog
          git diff --exit-code -- catalog/ \
            || { echo "::error::catalog/catalog.json is stale. Run 'make build-catalog' and commit."; exit 1; }

      - name: Run unit tests
        run: uv run --no-project --with requests python -m unittest discover -s tests -v

      - name: Run routing benchmark
        run: make benchmark

      - name: Build documentation
        run: uvx --from mkdocs --with 'mkdocs-material==9.5.*' --with pymdown-extensions mkdocs build --strict --site-dir site
